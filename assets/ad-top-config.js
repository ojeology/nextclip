/* BRYME top slot - DISABLED until the owner's single-widget native code lands.
   When it arrives this file gets:
   window.BRYME_TOP = { invoke: '<invoke.js URL>', container: '<container-...>' };
   (The multi-widget container unit already runs at the BOTTOM via the component.) */
