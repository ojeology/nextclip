#!/usr/bin/env node
const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');
const leagueFile=process.argv[2] || 'premier-league'; const d=JSON.parse(fs.readFileSync(path.join(root,'content/club-history',leagueFile+'.json'),'utf8')); const leagueSlug=leagueFile;
const esc=s=>String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const layout=(title,body)=>`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} | BRYME Sports</title><link rel="canonical" href="https://bryme.onrender.com/sports/clubs/${leagueSlug}/"><link rel="stylesheet" href="/assets/site.css"></head><body><header class="top"><div class="shell"><a class="brand" href="/">BRY<b>ME</b></a></div></header>${body}</body></html>`;
const cards=d.clubs.map(c=>`<article class="vcat"><b>${esc(c.name)}</b><span>Founded: ${esc(c.founded)} · ${esc(c.city)}</span><span>Home: ${esc(c.stadium)}</span><a class="quiet-link" href="${esc(c.source)}" target="_blank" rel="noopener">Club history source ↗</a></article>`).join('');
const body=`<main class="shell"><div class="crumb"><a href="/sports/">BRYME Sports</a> / Clubs / Premier League</div><section class="hero"><div class="eyebrow">Club history · research batch 1</div><h1>${esc(d.league)} clubs</h1><p class="lead">Founding year, home city and stadium for the current 2026/27 Premier League clubs. Sources are linked on each record.</p></section><section class="section"><div class="vnote">Last checked: ${d.lastChecked}. Major-honours totals are not displayed until BRYME reconciles each club against official club and competition honours records. We will not publish guessed silverware totals.</div><div class="vcat-grid" style="margin-top:20px">${cards}</div></section></main>`;
const out=path.join(root,'sports/clubs',leagueSlug,'index.html');fs.mkdirSync(path.dirname(out),{recursive:true});fs.writeFileSync(out,layout(d.league+' Clubs',body));
console.log('Built '+d.clubs.length+' '+d.league+' club records.');
