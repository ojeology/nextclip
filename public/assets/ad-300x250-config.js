/* BRYME 300x250 advertising unit — provider configuration.
   Served as an EXTERNAL same-origin file on purpose: the site's
   Content-Security-Policy forbids inline scripts, so the provider's
   `atOptions` assignment lives here instead of an inline <script> tag.
   Values are EXACTLY as supplied by the owner (12 Sep 2026) — key, format,
   dimensions and params are unaltered. */
window.atOptions = {
  'key': '51fc16f82ddc690721deee07bcd8bccd',
  'format': 'iframe',
  'height': 250,
  'width': 300,
  'params': {}
};
